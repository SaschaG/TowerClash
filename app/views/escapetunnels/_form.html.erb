<%= form_for(@escapetunnel) do |f| %>
  <% if @escapetunnel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@escapetunnel.errors.count, "error") %> prohibited this escapetunnel from being saved:</h2>

      <ul>
      <% @escapetunnel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rate %><br />
    <%= f.number_field :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
